<div>
  <nav class="px-2 bg-stone-600">
    <div class="container flex flex-wrap items-center justify-between mx-auto">
      <a href="/" class="flex items-center">
        <i class="fa-solid fa-earth-europe text-white pr-4"></i>
        <span class="self-center text-xl whitespace-nowrap dark:text-white">Always Reasonable</span>
      </a>
      <button data-collapse-toggle="navbar-dropdown" type="button" class="inline-flex items-center p-2 ml-3 text-sm text-gray-500 rounded-lg md:hidden hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-gray-200 dark:text-gray-400 dark:hover:bg-gray-700 dark:focus:ring-gray-600" aria-controls="navbar-dropdown" aria-expanded="false">
        <span class="sr-only">Open main menu</span>
        <svg class="w-6 h-6" aria-hidden="true" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" d="M3 5a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zM3 10a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zM3 15a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1z" clip-rule="evenodd"></path></svg>
      </button>
      <div class="hidden w-full md:block md:w-auto bg-stone-600" id="navbar-dropdown">
        <ul class="flex flex-col p-4 mt-4 border bg-stone-600  rounded-lg md:flex-row md:space-x-8 md:mt-0 md:text-sm md:font-medium md:border-0 dark:border-gray-700">
          <li>
            <%= link_to 'Home', root_path,
                        class: "block px-4 py-2 hover:text-white"%>
          </li>
          <li>
            <%= link_to 'Categories/Products', categories_path,
                        class: "block px-4 py-2 hover:text-white"%>
          </li>
          <% if user_signed_in? %>
            <li>
              <%= link_to "Account #{current_user.first_name}", root_path,
                          class: "block px-4 py-2 text-violet-300"%>
            </li>
          <% end  %>
          <% if !user_signed_in? %>
            <li>
              <%= link_to 'Sign in', user_session_path,
                          class: "block px-4 py-2 hover:text-white"%>
            </li>
          <% end %>
          <% if current_user %>
            <li>
                <%= link_to "Log out", destroy_user_session_path, method: :delete, data: { turbo_method: :delete },
                          class: 'block px-4 py-2 hover:text-white' %>
            </li>
          <% end %>

            <li>
              <%=  link_to 'Cart', show_cart_path,
                               class: "block px-4 py-2 text-sm hover:text-white" %>
            </li>

          <% if user_signed_in? %>
            <li>
              <%=  link_to 'Orders', orders_path,
                           class: "block px-4 py-2 text-sm hover:text-white" %>
            </li>
          <% end %>

          <% if !user_signed_in? %>
          <li>
                <%=  link_to 'Sign up', new_user_session_path,
                        class: "block px-4 py-2 text-sm text-green-200 hover:text-white"  %>
          </li>
          <%  end %>
        </ul>
      </div>
    </div>
  </nav>
</div>